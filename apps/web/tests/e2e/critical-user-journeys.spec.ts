import { describe, it } from 'vitest'

// E2E scaffolding placeholder.
// Real browser automation runner (Playwright/Cypress) should replace these TODOs.
describe('critical user journeys (e2e scaffold)', () => {
  it.todo('signup -> login -> onboarding progression')
  it.todo('create funnel -> edit -> publish')
  it.todo('capture lead -> analytics event emitted')
  it.todo('admin access guard for non-admin user')
})
