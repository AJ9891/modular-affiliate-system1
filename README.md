# Affiliate Launchpad

A high-converting React app demo for affiliate marketers.  
Built with Vite + React + Tailwind, configured for GitHub Pages deployment.

## 🚀 Setup

```bash
npm install
npm run dev
```

## 🧠 Build and Deploy

```bash
npm run build
npm run deploy
```

This publishes to: https://aj9891.github.io/affiliate-launchpad/

---

Tailwind + localStorage + client-side mock checkout.
For real payment integration (Stripe) or PDF generation, extend the component `src/AffiliateLaunchpad.jsx`.
